package challenges;

public class code_detector {
    public static void main(String[] args) {
        
        int mark = 20 ; 

        // String ternary = mark >= 80 ? "High" : (mark >= 50 ? "mordaret": "low");

        String sanketMark = mark>=80 ? "Hight" : (mark>=50 ? "2nd Ranker" : "low");
// System.out.println(ternary);
System.out.println(sanketMark);
    }
}
